<div class="logo2">
      ANYO
      </div>
<div class="topname">
  Hello <%= @username %>
</div>


<div class="box">

    <ul>
        <% @orders.each do |order|%>
          You ordered the following items:
          <%  order.order_items.each do |order_item|%>
         <li>  <%= order_item.candy.name %> <%= link_to 'Confirm order', my_orders_orders_path %></li>
          <%end%>
    <%end%>
     </ul>

    <% if !@candies.last.nil? %>

      <div class="box2">You have the following items on sale:

        <li><%= @candies&.last&.name %> - <%= @candies&.last&.availability %> box(es)
                <%= link_to "❌", candy_path(@candies&.last),
                          method: :delete,
                          data: { confirm: "Are you sure?" } %>
        <%= link_to "🖌", edit_candy_path(@candies&.last) %></li>
      </div>
    <% end %>
</div>

